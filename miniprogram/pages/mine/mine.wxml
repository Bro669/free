<view class="card stats-card" wx:if="{{stats}}">
  <view class="stat-col"><text class="stat-num">{{stats.count}}</text><text class="stat-label">骑行次数</text></view>
  <view class="stat-col"><text class="stat-num">{{stats.distanceText}}</text><text class="stat-label">总里程</text></view>
  <view class="stat-col"><text class="stat-num">{{stats.durationText}}</text><text class="stat-label">总时长</text></view>
</view>

<view class="card" wx:if="{{letters.length}}">
  <view class="collect-title">字母收集册 <text class="collect-sub">把 26 个字母骑亮</text></view>
  <view class="letter-grid">
    <view class="letter {{item.got ? 'got' : ''}}" wx:for="{{letters}}" wx:key="c">{{item.c}}</view>
  </view>
  <view class="extras" wx:if="{{extras.length}}">
    <text class="collect-sub">特别收藏：</text>
    <text class="extra-chip" wx:for="{{extras}}" wx:key="*this">{{item}}</text>
  </view>
</view>

<view class="tabs">
  <view class="tab {{tab === 'routes' ? 'active' : ''}}" data-tab="routes" bindtap="switchTab">我的路线</view>
  <view class="tab {{tab === 'rides' ? 'active' : ''}}" data-tab="rides" bindtap="switchTab">我的骑行</view>
</view>

<block wx:if="{{tab === 'routes'}}">
  <view class="card item" wx:for="{{routes}}" wx:key="_id" data-id="{{item._id}}" bindtap="openRoute">
    <image wx:if="{{item.thumb}}" class="item-thumb" src="{{item.thumb}}" mode="aspectFill" />
    <view wx:else class="item-badge">{{item.text}}</view>
    <view class="item-info">
      <view class="item-name">{{item.name}}<text class="private" wx:if="{{!item.isPublic}}">私密</text></view>
      <view class="item-meta">{{item.distanceText}} · {{item.dateText}}</view>
    </view>
    <view class="arrow">›</view>
  </view>
  <view class="empty" wx:if="{{!routes.length && !loading}}">
    <view>{{loadError ? '加载失败，请确认云环境配置' : '还没有路线'}}</view>
    <view class="btn-primary empty-btn" bindtap="goDesign">去设计一条</view>
  </view>
</block>

<block wx:else>
  <view class="card item" wx:for="{{rides}}" wx:key="_id" data-id="{{item._id}}" bindtap="openRide">
    <view class="item-badge ride">{{item.text}}</view>
    <view class="item-info">
      <view class="item-name">{{item.distanceText}}</view>
      <view class="item-meta">{{item.durationText}} · {{item.dateText}}</view>
    </view>
    <view class="arrow">›</view>
  </view>
  <view class="empty" wx:if="{{!rides.length && !loading}}">还没有骑行记录</view>
</block>
